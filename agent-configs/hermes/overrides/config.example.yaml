# Example only. Do not paste secrets into this file.
model:
  default: auto
  provider: custom:blockrun
  base_url: http://127.0.0.1:8402/v1

agent:
  max_turns: 60
  reasoning_effort: medium

toolsets:
  - hermes-cli
