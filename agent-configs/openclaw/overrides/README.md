# OpenClaw Overrides

Use this folder for OpenClaw-specific prompts and plugin notes.

V1 does not assume OpenClaw is installed. If it is installed, connect only the plugins you actually use and keep API keys in local ignored env files.
